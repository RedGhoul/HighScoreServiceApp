@model Domain.Entities.Game

@{
    ViewData["Title"] = "Details";
}
<div class="row">
    <div class="col-3"></div>
    <div class="col-6">
        <h2>Details for <a style="color:crimson">@Model.Name</a></h2>
        <div>
            <hr />
            <dl class="row">
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Name)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Name)
                </dd>
                <dt class="col-sm-2">
                    @Html.DisplayNameFor(model => model.Identifier)
                </dt>
                <dd class="col-sm-10">
                    @Html.DisplayFor(model => model.Identifier)
                </dd>
            </dl>
        </div>
        <div>
            <a class="btn btn-warning" asp-action="Edit" asp-route-id="@Model.Id">Edit</a> |
            <a class="btn btn-primary" asp-action="ViewAllGames">Back to List</a>
        </div>
    </div>
    <div class="col-2"></div>
</div>



